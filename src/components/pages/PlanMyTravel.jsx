import { Text, Container } from "../atoms";
import { MyFav } from "../templates"

const PlanMyTravel = () => {
    return (
        <Container.Page>
            <Text.Title style={{ marginBottom: "60px" }}>Planifier mon voyage</Text.Title>
        </Container.Page>

    );
};

export default PlanMyTravel;