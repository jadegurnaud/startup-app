import React from "react";
import { DOM } from "../../nanites";

const SubTitle = (props) => {
    return <DOM.StyledSubTitle className="soustitre" {...props}></DOM.StyledSubTitle>;
}

export default SubTitle;